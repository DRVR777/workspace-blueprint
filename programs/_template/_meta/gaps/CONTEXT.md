# Gap Registry — {{PROJECT_NAME}}

## Scope: {{PROJECT_NAME}}-internal only
Cross-project gaps go to `{root}/_meta/gaps/`.

## Current Open Gaps

| Gap ID | Type | detected_in | Description | Severity | Status |
|--------|------|-------------|-------------|----------|--------|
| [generated by prd-intake.md] | | | | | |

## How Gaps Become Tasks

1. Agent logs inference to `pending.txt`
2. Gap detection agent classifies it (`{root}/_meta/gap-detection-agent.md`)
3. A gap JSON object is created in this folder
4. The `proposed_expansion` field becomes the task description
5. Feed that to `{root}/_meta/ur-prompt.md` Steps 1–7
6. Execute the resulting prompt — that closes the gap

## Resolution Protocol

**Blocking gaps (PRD unknowns / assumption ADRs):**
Do not write code in the affected program until closed.
Close by: writing an accepted ADR in `_planning/adr/`, updating gap status here.

**Degrading/cosmetic gaps:**
Follow standard session protocol in `{root}/_meta/runner.md`.

## Escalation Rule
If a gap involves anything outside this project's folder → `{root}/_meta/gaps/pending.txt` instead.
