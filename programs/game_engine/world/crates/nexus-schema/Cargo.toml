[package]
name = "nexus-schema"
description = "NEXUS schema registry: content-addressed schema IDs, JSON descriptors, dynamic discovery"
version.workspace = true
edition.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
